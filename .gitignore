# ไฟล์ข้อมูลภายใน — ไม่ push ขึ้น public repo
*.xlsx
~$*

# ไฟล์เฉพาะเครื่อง
.claude/
Thumbs.db
desktop.ini

# รหัสผ่านผู้ใช้ (ห้ามขึ้น git เด็ดขาด)
credentials.local.csv
*.local.csv

# preview/design artifacts
landing-preview.html
logo-app-crop.png
logo app.png
landing page.png
hero.png

# ไฟล์ mockup/ตัวอย่าง (เปิดดูในเครื่อง)
mockups/
real-budget-page.html
import-src-2026.json
